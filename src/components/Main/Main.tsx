import React, {FC} from 'react';
import './main.scss'

const Main: FC = () => {
    return (
        <main>
            <div className={'container'}>
                Main
            </div>
        </main>
    );
};

export default Main;
